<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title }}</title>
  <link rel="stylesheet" href="/styles/main.css">
</head>
<body>
  <header>
    <a href="/">← Back to home</a>
  </header>
  <main>
    <h1>{{ title }}</h1>

    {% if image %}
      <figure>
        {% image image, image_alt, "lazy", image_class %}
        {% if image_alt %}
          <figcaption>{{ image_alt }}</figcaption>
        {% endif %}
      </figure>
    {% endif %}

    <article>
      {{ content | safe }}
    </article>
  </main>
</body>
</html>
