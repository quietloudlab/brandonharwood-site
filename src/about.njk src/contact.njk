<div class="min-h-screen flex flex-col md:flex-row">
  <!-- Sidebar -->
  <aside class="relative md:fixed bg-white/[0.01] backdrop-blur-xl md:sticky md:top-0 w-full md:w-60 p-4 navigation z-40 md:h-screen flex flex-col border-b md:border-b-0 border-[#C0C0C0] mb-8 md:mb-0">
    <div class="flex flex-col flex-1 min-h-0">
      <div class="mt-0">
        <!-- Back Button -->
        <button 
          onclick="handleBack()"